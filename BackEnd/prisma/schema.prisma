datasource db {
  provider = "postgresql"
  url      = "postgresql://neondb_owner:CH0gfoxqIW8l@ep-silent-dust-a5soezec-pooler.us-east-2.aws.neon.tech/uts?sslmode=require"
    // url      = env("DATABASE_URL")

}

generator client {
  provider = "prisma-client-js"
}

model User {
  id       Int     @id @default(autoincrement())
  name     String
  email    String  @unique
  password String
}
